<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.EscolaVisionApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.EscolaVisionApp" parent="Base.Theme.EscolaVisionApp">
        <item name="android:statusBarColor">@color/fondoInicio</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>
</resources>